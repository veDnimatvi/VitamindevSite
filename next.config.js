/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["www.monterail.com"],
    // domains: ["localhost"],
  },
};

module.exports = nextConfig;
